$Id$

Program			Function	
-------			--------
colourise		convert letterspace fasta files to colourspace
finalpass		parse rmapper output (non-pretty-printed output)
prettyprint-cs		pretty print alignments from finalpass (colourspace)
prettyprint-ls		pretty print alignments from finalpass (letterspace)
revcmpl			generate reverse complements of fasta reftigs
rmapper-cs		colourspace read mapper
rmapper-ls		letterspace read mapper
splitreads		split fasta read files into smaller chunks
splittigs		split fasta contig files into one file per contig
